# SD Search API

## Description

The SD Search API enables search across different datasets. At first,
it will support Bigpicture image search.

## Development

Launch the Postgres database by running:

`docker compose --profile dev up --build`
